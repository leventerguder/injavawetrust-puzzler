package chapter10.puzzle94;

import java.util.*;

public class ShuffleFixed {

    // Use the shuffle method provided by the library:
    public static void shuffle(Object[] a) {
        Collections.shuffle(Arrays.asList(a));
    }
}
